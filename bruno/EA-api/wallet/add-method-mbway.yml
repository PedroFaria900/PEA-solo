info:
  name: add-method-mbway
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseUrl}}/api/carteira/metodos"
  body:
    type: json
    data: |-
      {
        "tipo": "MBWAY",
        "telemovel": "912345678"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
