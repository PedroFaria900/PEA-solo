info:
  name: list-transactions
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/carteira/transacoes"
  params:
    - name: page
      value: "0"
      enabled: false
    - name: size
      value: "20"
      enabled: false

  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
