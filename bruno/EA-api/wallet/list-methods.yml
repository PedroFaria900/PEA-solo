info:
  name: list-methods
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/carteira/metodos"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
