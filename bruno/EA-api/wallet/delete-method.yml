info:
  name: delete-method
  type: http
  seq: 9

http:
  method: DELETE
  url: "{{baseUrl}}/api/carteira/metodos/{{metodoId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
