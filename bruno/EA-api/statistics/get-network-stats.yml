info:
  name: get-network-stats
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/admin/estatisticas/rede"

  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
