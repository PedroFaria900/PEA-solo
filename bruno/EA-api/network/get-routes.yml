info:
  name: get-routes
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/rotas"

  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
