info:
  name: get-lines
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/linhas"

  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
