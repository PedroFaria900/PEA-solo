info:
  name: validate-token
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/validacoes"
  body:
    type: json
    data: |-
      {
        "tituloId": "eyJhbGciOiJIUzI1NiIsInR...",
        "leitorId": "123e4567-e89b-12d3-a456-426614174000"
      }

  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
