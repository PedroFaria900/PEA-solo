info:
  name: get-trips
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/viagens"

  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
