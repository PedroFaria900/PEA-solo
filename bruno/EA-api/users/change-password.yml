info:
  name: change-password
  type: http
  seq: 3

http:
  method: PUT
  url: "{{baseUrl}}/api/utentes/perfil/password"
  body:
    type: json
    data: |-
      {
        "passwordAtual": "password123",
        "passwordNova": "novaSenha123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
