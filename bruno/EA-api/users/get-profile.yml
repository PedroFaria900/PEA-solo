info:
  name: get-profile
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/utentes/perfil"

  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
