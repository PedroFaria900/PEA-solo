info:
  name: get-titles
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/titulos"

  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
